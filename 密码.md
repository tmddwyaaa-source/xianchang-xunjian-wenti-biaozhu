# 本机演示账号

仅用于本机联调。密码已 bcrypt 存库，这里是**明文种子**，方便登录管理端和手机 App。

管理端：http://localhost:5174  
后端：http://127.0.0.1:8080（手机请填电脑局域网 IP，不要用 localhost。若 8080 被占用则改用 8081）

| 用户名 | 密码 | 角色 | 能做什么 |
|--------|------|------|----------|
| `admin` | `admin123` | 管理员 | 看列表、改状态、删除任意问题、提交 |
| `inspector` | `inspect123` | 巡检员 | 看列表、提交新问题、删除自己交的；不能改状态 |
| `viewer` | `view123` | 只读 | 只能看列表 |

Unity 手机端建议用 `inspector` / `inspect123`。

说明：只有 `users` 表为空时后端才会写入上述三个账号。若登录失败，多半是库里已有用户，或后端没在 8080 启动。
